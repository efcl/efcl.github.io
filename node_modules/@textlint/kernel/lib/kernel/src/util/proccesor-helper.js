// LICENSE : MIT
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var assert = require("assert");
/**
 * find processor with `ext`
 * @param {Processor[]} processors
 * @param {string} ext
 * @returns {Processor}
 */
function getProcessorMatchExtension(processors, ext) {
    var matchProcessors = processors.filter(function (processor) {
        // static availableExtensions() method
        var processorConstructor = processor.constructor;
        assert(typeof processorConstructor.availableExtensions === "function", "Processor(" + processorConstructor.name + " should have availableExtensions()");
        var extList = processorConstructor.availableExtensions();
        return extList.some(function (targetExt) { return targetExt === ext || "." + targetExt === ext; });
    });
    if (matchProcessors.length) {
        return matchProcessors[0];
    }
    return null;
}
exports.getProcessorMatchExtension = getProcessorMatchExtension;
//# sourceMappingURL=proccesor-helper.js.map