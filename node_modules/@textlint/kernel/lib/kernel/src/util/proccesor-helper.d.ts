import { TextlintPluginProcessor } from "../textlint-kernel-interface";
/**
 * find processor with `ext`
 * @param {Processor[]} processors
 * @param {string} ext
 * @returns {Processor}
 */
export declare function getProcessorMatchExtension(processors: TextlintPluginProcessor[], ext: string): TextlintPluginProcessor | null;
