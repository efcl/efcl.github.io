---
title: "ni.zsh v1.8.0リリース: Socket Firewallによるパッケージセキュリティ保護機能を追加"
author: azu
layout: post
date : 2025-10-08T09:00
category: JavaScript
tags:
    - npm
    - yarn
    - pnpm
    - bun
    - zsh
    - security

---

npm/yarn/pnpm/bunを同じコマンドで扱える[ni.zsh](https://github.com/azu/ni.zsh)のv1.8.0をリリースしました！

- [Release v1.8.0 · azu/ni.zsh](https://github.com/azu/ni.zsh/releases/tag/v1.8.0)

このバージョンでは、Socket Firewallを統合し、パッケージのインストールと実行時にサプライチェーン攻撃から保護する機能を追加しました。

ni.zshについては、次の記事を参照してください。

- [npm/yarn/pnpm/bunを同じコマンドで扱える ni のzsh実装を書いた | Web Scratch](https://efcl.info/2023/03/24/ni/)
- [ni.zsh: npmインストール時のサプライチェーン攻撃を検知する機能を追加 | Web Scratch](https://efcl.info/2023/08/29/ni.zsh-socket.dev/)

## ni.zsh v1.8.0の変更点

主要な変更点は次の通りです。詳細はリリースノートを参照してください。

- [Release v1.8.0 · azu/ni.zsh](https://github.com/azu/ni.zsh/releases/tag/v1.8.0)

### 🛡️ Socket Firewallの統合

Socket Firewallを統合し、パッケージのインストールと実行時にリアルタイムでセキュリティスキャンを行えるようになりました。

Socket Firewallは、HTTPプロキシとしてパッケージマネージャのネットワーク通信をインターセプトし、悪意のあるパッケージがダウンロードされる前にブロックするツールです。

従来のni.zshでは、Socket.dev APIを使ってリスクスコアを取得し、インストール前に確認を求める仕組みでした。
Socket Firewallでは、ネットワークレベルでブロックするため、より確実にマルウェアパッケージの侵入を防げます。

- 既知のマルウェアパッケージは自動的にブロック
- AI検知された潜在的なマルウェアは警告を表示
- パッケージマネージャに関係なく統一的な保護を提供

詳しくは次の記事を参照してください。

- [Introducing Socket Firewall: Free, Proactive Protection for Your Software Supply Chain](https://socket.dev/blog/introducing-socket-firewall)

### セットアップ方法

Socket Firewallの機能を使用するには、次の手順でセットアップします。

1. Socket Firewallをグローバルにインストール

```bash
npm i -g sfw
```

2. `.zshrc` に環境変数を追加

```bash
export NI_USE_SOCKET_FIREWALL=1
```

設定後、以下のコマンドでSocket Firewallによる保護が有効になります。

### 保護対象のコマンド

次のコマンドでSocket Firewallによる保護が有効になります。

- `ni` / `ni add <pkg>` - パッケージのインストール
- `ni exec <cmd>` / `ni dlx <pkg>` - パッケージの実行

```bash
# パッケージインストール時に保護
ni add express

# 直接実行する場合も保護
ni dlx create-vite
```

マルウェアパッケージがブロックされると、次のように表示されます。

```
=== Socket Firewall ===
- Blocked npm package: name: lodahs; version: 1.0.0
```

### カスタムバイナリパスの指定

デフォルトでは`sfw`コマンドを使用しますが、カスタムのSocket Firewallバイナリパスを指定することもできます。

```bash
export NI_SOCKET_FIREWALL_BIN=/path/to/custom/sfw
```

## なぜSocket Firewallを統合したか

最近のnpmエコシステムでは、サプライチェーン攻撃が急増しています。

過去には[event-stream攻撃](https://blog.npmjs.org/post/180565383195/details-about-the-event-stream-incident)のような稀な事例でしたが、最近では次のような有名パッケージの侵害が相次いで発生しています。

- tinycolor
- chalk
- nx
- eslint-config-prettier

これらの攻撃では、メンテナが乗っ取られたり、typosquattingで偽装されたパッケージが公開されたりしています。
従来の対策として、`--ignore-scripts`でpostinstallスクリプトを無効化する方法がありますが、正常なパッケージの動作にも影響が出るため、デフォルトでは使いづらい問題があります。

Socket Firewallを統合することで、開発者のマシン上で以下のような保護が可能になりました。

- ネットワークレベルでマルウェアパッケージをブロック
- パッケージマネージャに関係なく統一的な保護
- 既知のマルウェアだけでなく、AI検知された潜在的なマルウェアも警告

ni.zshは複数のパッケージマネージャ（npm、yarn、pnpm、bun）をラップするため、どのパッケージマネージャを使用していてもSocket Firewallによる保護を受けられます。

## Socket.dev APIとの違い

ni.zshでは、以前から[Socket.dev API](https://efcl.info/2023/08/29/ni.zsh-socket.dev/)を使ったマルウェア検知機能を提供していました。

Socket.dev APIは、パッケージのリスクスコアを取得して、インストール前に確認を求める仕組みでした。
これにより、typosquattingや疑わしいパッケージを検知できましたが、最終的なインストール判断はユーザーに委ねられていました。

一方、Socket Firewallは次のような違いがあります。

| 機能 | Socket.dev API | Socket Firewall |
|------|----------------|-----------------|
| 検知方法 | APIでリスクスコアを取得 | HTTPプロキシでネットワーク通信をインターセプト |
| 保護レベル | インストール前に確認を求める | 既知のマルウェアは自動的にブロック |
| セットアップ | APIキーが必要 | APIキー不要（無料） |
| 対応パッケージマネージャ | npm/yarn/pnpm/bun | npm/yarn/pnpm/pip/uv/cargo |

Socket Firewallは無料で使えて、APIキーも不要なため、よりシンプルに使えるようになりました。
Socket.dev APIによる検知機能は引き続き利用可能で、併用することでより高度な保護が可能です。

## まとめ

ni.zsh v1.8.0では、Socket Firewallを統合してパッケージのセキュリティ保護機能を追加しました。

Socket Firewallを有効にするには、`sfw`をインストールして`NI_USE_SOCKET_FIREWALL=1`を設定するだけです。
従来のSocket.dev API機能も引き続き利用可能で、併用することでより高度な保護が可能です。

フィードバックがありましたら、GitHubのIssueでお知らせください。

- [Issues · azu/ni.zsh](https://github.com/azu/ni.zsh/issues)
